[pytest]
markers =
    integration: marks tests that require Ollama running locally (deselect with -m "not integration")
    llm_judge: marks tests that use LLM-as-a-Judge (requires Ollama running locally)